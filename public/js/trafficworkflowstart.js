/*document.addEventListener('DOMContentLoaded', function() {
    // Leer los valores de sessionStorage si no vienen de la URL
    const origin = sessionStorage.getItem('shipment_origin');
    const trailerId = sessionStorage.getItem('trailer_id');

    if (origin) {
        document.getElementById('inputorigin').value = origin;
    }

    if (trailerId) {
        document.getElementById('inputidtrailer').value = trailerId;
    }
});*/